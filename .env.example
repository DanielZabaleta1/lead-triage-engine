# Supabase (same project as Power Flow OS, schema `triage` is isolated from the rest)
SUPABASE_URL=
SUPABASE_SERVICE_KEY=

# Gemini API — needs its own GCP project, NOT the one shared with n8n/powerflow-insights
# (that one's capped at 20 requests/day for gemini-2.5-flash and is already in production
# use elsewhere — reusing it here risks starving those systems). See pasos_daniel.md.
GEMINI_API_KEY=

ADMIN_TOKEN=
